{
  "name": "polygon-fees",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ dmihal/polygon-fees",
    "create-local": "graph create --node http://localhost:8020/ dmihal/polygon-fees",
    "remove-local": "graph remove --node http://localhost:8020/ dmihal/polygon-fees",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dmihal/polygon-fees"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.18.0",
    "@graphprotocol/graph-ts": "0.18.0"
  }
}
